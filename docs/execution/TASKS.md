# Flownee Tasks

Keep exactly one implementation task under **Now**. Move completed items to **Done** with test, commit, deployment, or screenshot evidence.

## Now

- [ ] Continue the optional post-submission representative usability study using `docs/evaluation/USABILITY_TEST.md`, recording only observed outcomes as sessions complete.

## Next

- None. The submitted hackathon MVP has no remaining implementation or release gate.

## Blocked

- None.

## Done

- [x] Removed the production build's Google Fonts network dependency while preserving Plus Jakarta Sans. Evidence: the official full variable WOFF2 and SIL OFL 1.1 license are pinned to upstream commit `18d1cd2f7ea10481919d2f05c1f7064b7307fc26`; `next/font/local` preserves the existing CSS variable, normal style, and 200–800 range; a clean build after deleting `.next` succeeds without network escalation; source and fresh build contain no `next/font/google`, `fonts.googleapis`, or `fonts.gstatic` references; the bundled output contains the expected single 60,612-byte font; 35 test files with 133 tests, lint, standalone TypeScript validation, and the optimized production build pass.

- [x] Added technical architecture diagram to the repository and README. Evidence: matching PNG and SVG assets under `docs/technical/`; the README displays the PNG and links to the scalable SVG; the diagram shows local IndexedDB state, temporary audio handling, protected Netlify routes, server-only credentials, GPT-4o Transcribe, GPT-5.6 structured planning, and last-valid-plan recovery.

- [x] Submitted Flownee to OpenAI Build Week before the deadline. Evidence: public Devpost entry at https://devpost.com/software/flownee; team submission confirmation; public page independently returned HTTP 200 on 2026-07-21.

- [x] Closed every MVP acceptance criterion after final testing. Evidence: team confirmation that the complete test matrix succeeded; public voice-to-plan and task-management journey; reload/reopen persistence; failure recovery; credential boundary; Chrome desktop, Chrome Android, Edge, and best-effort Safari coverage; 35 test files with 133 tests, lint, standalone TypeScript validation, and the optimized production build pass. The ongoing representative usability study is optional post-submission research, not a Build Week submission requirement or MVP release gate.

- [x] Resolved the production PostCSS security advisory. Evidence: pnpm 11's supported workspace-level override pins every PostCSS path to patched `8.5.19`; the lockfile removes vulnerable `8.4.31` and redirects Next.js without changing unrelated dependency resolutions; frozen-lockfile installation and supply-chain policy checks pass; `pnpm audit --prod` reports no known vulnerabilities; 35 test files with 133 tests, lint, standalone TypeScript validation, and the optimized production build pass.

- [x] Reconciled the product validation and next-action language narratives. Evidence: README, project brief, and usability documentation now record multi-stage concept and independent qualitative validation, including user-reported reductions in forgotten everyday tasks and mental clutter plus greater day-to-day satisfaction; the claim boundary explicitly avoids invented participant counts, measurements, effect sizes, or causality; **What should I do now?**, **What makes sense next?**, and **Do this now** are documented as intentional contextual variants of one next-action concept rather than inconsistent functionality.

- [x] Made every URL-driven demonstration state self-explanatory in the UI. Evidence: `?demo=sample`, `updating`, `complete`, and `loading` now show a compact accessible **Read-only preview** banner with a Preview badge, information icon, explicit no-save/no-AI wording, and a 44px-or-larger **Try interactive Flownee** link; the voice capture is not mounted in demo mode, guaranteeing no recording or AI request from the preview; the blocking updating fixture carries the same explanation inside its modal; the normal home URL retains voice capture and no banner; browser checks at 320px and 430px confirm no horizontal overflow, and 35 test files with 133 tests, lint, and the optimized production build pass.

- [x] Strengthened the final Design, Potential Impact, and Quality of the Idea evidence without overstating research. Evidence: README and judge instructions now explain why fictional demo states are intentionally read-only; the design system documents the one-time Hyper Text scramble as a deliberate modern metaphor for scattered thoughts resolving into flow; the project brief sharpens Flownee's quick, simple, freedom-from-system-maintenance differentiation; and the usability record summarizes the Build Week iteration cycle and concrete functional/design improvements while retaining the boundary that no representative-participant study was completed.

- [x] Completed the final submission-evidence package. Evidence: Codex `/feedback` Session ID `019f709a-b5fb-7230-bf41-4c4508f7051f`; public 2:36 YouTube demonstration at `https://youtu.be/xpT-oI0Lh4M`; completed Devpost draft; team-confirmed intended-platform acceptance, media/asset rights, judge-period API credits, and signed-out URL audit; independent signed-out checks confirmed the public application, fictional sample, enabled AI-status route, and YouTube playback page.

- [x] Finalized the README. Evidence: the README now explains the problem and product, live and read-only demo paths, complete feature set, GPT-4o Transcribe/GPT-5.6 workflow, strict planning contract, local-first reliability and privacy boundaries, technology stack, cross-platform setup, environment variables, automated/manual verification, honestly bounded platform evidence, detailed Codex acceleration, explicit human decisions, repository map, documentation index, and MIT license; all relative targets and required submission topics validate, `git diff --check` passes, and the README sample-data and Codex-narrative compliance gates are PASS.

- [x] Added testing instructions for the public deployment. Evidence: root `TESTING_INSTRUCTIONS.md` covers account-free setup, a fictional two-intention voice journey, transcript and interpretation review, non-deterministic AI expectations, task completion/postponement/restoration/edit/delete, automatic replanning, reload/reopen persistence, two-step delete-all, read-only demo URLs, recovery/privacy behavior, and honestly bounded physical-device evidence; README links the guide and the compliance testing-instructions gate is PASS; Markdown diff validation passes.

- [x] Completed product-owner mobile acceptance of the current public release. Evidence: owner-reported physical testing on iPhone 17, iPhone 12, and iPhone 8 in current Safari, Chrome, and Firefox, plus Redmi Note 12 Pro 5G in installed-app mode and current Chrome/Firefox; microphone recording, transcript and interpretation review, successful live AI interaction/replanning, all task actions, individual edit/delete, delete-all, reload, and reopen persistence worked as expected with no reported failures. Exact OS/browser versions and per-browser MIME diagnostics were not captured, and this is not representative-participant usability evidence.

- [x] Restored standalone TypeScript compatibility for the server error-path tests. Evidence: the three Vitest `toMatchObject` assertions now use `satisfies Partial<...>` instead of unsupported matcher type arguments, preserving compile-time checking without changing runtime expectations; standalone `tsc --noEmit`, 35 test files and 130 tests, lint, and the optimized production build pass.

- [x] Reduced the animated home heading from `18px` to `16px`. Evidence: `YOUR FLOW. WHAT MAKES SENSE NEXT` retains its centered uppercase Hyper Text reveal, medium weight, primary violet color, stable accessible label, and unchanged surrounding layout; local computed styles confirm `16px`, weight `500`, exact center alignment, one-line presentation within the mobile-width app shell, and zero horizontal overflow; 35 test files and 130 tests, lint, TypeScript, and the optimized production build pass. Commit and deployment await separate product-owner approval.

- [x] Made the shared application header sticky across every page. Evidence: the shared header now stays at the viewport top with its blurred surface and `z-40` stacking; the offline banner remains above it in document order and scrolls away naturally; task dialogs and blocking overlays retain higher stacking levels; Home, Help, Settings, Design System, and recording diagnostics all retain an exact `0px` header top after scrolling in the local browser, with correct light/dark surfaces and zero horizontal overflow; 35 test files and 130 tests, lint, TypeScript, and the optimized production build pass. Commit and deployment await separate product-owner approval.

- [x] Centered the animated home heading, aligned current-task badges, and completed the requested action-icon coverage. Evidence: `YOUR FLOW. WHAT MAKES SENSE NEXT` remains an uppercase 18px, 1600ms Hyper Text heading and now computes to exact center alignment; `Do this now` and `About 10′` both use the same 28px `important` badge treatment while retaining Sparkles and Clock icons; `Confirm and interpret`, every visible `Cancel`, `Add to my flow`, its saving state, and every `Revise transcript` action now pair text with meaningful icons without changing handlers or variants. Local sample measurements confirm a zero-pixel heading center delta, matching 28px badges, final heading copy, and zero horizontal overflow; 35 test files and 130 tests, lint, TypeScript, and the optimized production build pass. Commit and deployment await separate product-owner approval.

- [x] Replaced the production `Done` and `Do later` buttons with the approved slide-to-confirm controls. Evidence: the current-action row is ordered `Do later`, `Done`, then the fixed 44px `...` menu; two `minmax(0,1fr)` columns split all remaining width equally after the menu and two gaps; compact labels remain clear beside 48px thumbs; completion, postponement, replanning, blocking update feedback, completion celebration, error retry, and task management retain their existing handlers; the enabled `Done` slider alone keeps the five-second reduced-motion-safe Shine Border. Local live-flow inspection reports two equal 160.2px sliders, one 44px menu, one shine overlay, and zero horizontal overflow without triggering an action; 35 test files and 129 tests, lint, TypeScript, and the optimized production build pass. Commit and deployment await separate product-owner approval.

- [x] Added shadcn-inspired slide-to-confirm prototypes for current-task actions before production integration. Evidence: reusable 56px native-button controls use fully rounded tracks, circular 48px thumbs, subtle borders/shadows, and brand-colored progress fills while supporting pointer dragging, a 75% confirmation threshold, incomplete/cancel reset, Enter/Space confirmation, disabled and reduced-motion treatments, and one confirmation per mount; the `/design-system` previews use teal for `Done` and muted blue for `Do later`, mutate no task data, reset independently, remain readable in light/dark themes, and show zero horizontal overflow; local keyboard confirmation and reset pass; 35 test files and 129 tests, lint, TypeScript, and the optimized production build pass after one transient Google Fonts retry. The later production integration is recorded above.

- [x] Consolidated the home page title and added a calm Magic UI Hyper Text reveal. Evidence: `YOUR FLOW. WHAT MAKES SENSE NEXT` remains the page's single semantic `h1` at a uniform `18px`, medium weight, and primary violet color; the official MIT-licensed component is adapted with a `1600ms` one-time scramble, no hover replay, stable accessible naming, and a static reduced-motion path; local computed-style verification confirms the requested size, one-line presentation in the 430px shell, and zero horizontal overflow; 34 test files and 125 tests, lint, TypeScript, and the optimized production build pass. Commit and deployment await separate product-owner approval.

- [x] Removed the post-addition `Flow updated` success screen and made the handoff automatic. Evidence: after either a reviewed intention commit or a successful no-new-item capture, Flownee waits for the refreshed home snapshot and then closes the capture dialog without an extra tap; the committing state remains blocking until that handoff completes, while persistence failures still return to interpretation review; source audit finds zero remaining success-screen references; 34 test files and 124 tests, lint, and the optimized production build pass. Commit and deployment await separate product-owner approval.

- [x] Grouped postponed intentions before completed intentions in `Postponed and completed items`. Evidence: the component derives a stable display-only order with every `Later` item first and every `Done` item afterward, preserving the original order within each status group and leaving stored records untouched; read-only local verification shows one `Later` intention followed by two `Done` intentions with zero horizontal overflow; 34 test files and 123 tests, lint, and the optimized production build pass. Commit and deployment await separate product-owner approval.

- [x] Renamed `Saved items` to `Postponed and completed items` and removed its redundant local-storage sentence. Evidence: the section and clean-done confirmation use the same terminology, while status badges, completed-title treatment, management controls, clean-up, restoration, persistence, and privacy behavior remain unchanged; component rendering proves the new heading, absence of both former strings, retained two management controls, and unchanged crossed-out/postponed treatments; 34 test files and 123 tests, lint, and the optimized production build pass. Commit and deployment await separate product-owner approval.

- [x] Consolidated the upcoming-list heading into `Next items in your current flow`. Evidence: the former `Up next` title and dynamic `3 more items in your current flow` subtitle are removed, while the ordered intentions, clock-based effort metadata, empty state, and task controls remain unchanged; local sample verification shows the new heading on one line, neither former string in the section, and zero horizontal overflow; 34 test files and 123 tests, lint, and the optimized production build pass. Commit and deployment await separate product-owner approval.

- [x] Placed the current recommendation’s `Do this now` and time-effort labels next to each other. Evidence: the unchanged important badge is followed immediately by the existing `About 10′`-style effort badge in one responsive metadata row; the title now follows beneath both labels, while the optional updating badge remains supported; local sample measurements show identical vertical centers, correct order, and zero horizontal overflow; 34 test files and 123 tests, lint, and the optimized production build pass. Commit and deployment await separate product-owner approval.

- [x] Replaced the visible `Estimated` prefix in `Up next` with a compact clock icon. Evidence: each upcoming effort row retains the exact effort value, uses the existing `Clock3` visual cue, and preserves the meaning for assistive technology with screen-reader-only `Estimated effort` text; pending estimates keep their existing wording; local sample verification shows one clock beside `5′`, `30′`, and `10′`, no visible numeric `Estimated` prefix, and zero horizontal overflow; 34 test files and 123 tests, lint, and the optimized production build pass. Commit and deployment await separate product-owner approval.

- [x] Versioned the edited capture artwork as `main-brain-v2.png` to invalidate stale browser and Next.js image caches. Evidence: the original `main-brain.png` remains preserved; the versioned asset is an exact binary copy; the application and regression assertion use the new URL. Final local URL verification and build evidence are recorded in the matching session log. No commit, push, merge, or deployment occurred.

- [x] Switched the voice-capture base artwork to the product-owner-supplied `main-brain.png`. Evidence: the `1240x773` RGBA asset replaces only the fixed first-layer source; the existing responsive frame, four transparent intention overlays, two-second cycle, dialog spacing, and voice behavior remain unchanged; regression coverage locks the new path; light/dark local checks confirm the clouds and rotating details align, the dialog remains `430px` wide, and horizontal overflow is zero; both brief recording states were cancelled before transcription or upload; 34 test files and 123 tests, lint, and the optimized production build pass. No commit, push, merge, deployment, transcription, task mutation, or paid AI request occurred.

- [x] Replaced the voice-capture base artwork with the approved second Flownee brain and made its presentation genuinely transparent and evenly colored. Evidence: the original supplied `main 2.png` remains intact; one product-owner-approved `gpt-image-1.5` edit produced a native-alpha extraction, then deterministic palette cleanup flattened the brain to Flownee light blue while preserving the dark navy silhouette and internal linework; the application now references `/images/flownee/main-2-transparent.png` beneath the same four intention overlays and unchanged two-second cycle; light/dark local checks show the transparent asset without a rectangular background, distortion, clipping, or horizontal overflow in the `430px` dialog; both brief microphone sessions used for dialog inspection were cancelled before transcription or upload; 34 test files and 123 tests, lint, and the optimized production build pass. No commit, push, merge, deployment, transcription, task mutation, or planning request occurred.

- [x] Made the approved Shine Border treatment visibly repeat every five seconds. Evidence: the microphone and enabled current-task `Done` overlays make one soft pass during approximately the first 2.5 seconds, remain fully invisible for the rest of the five-second cycle, and repeat using CSS only; disabled `Done` still renders no overlay; reduced-motion suppression, `aria-hidden`, pointer-event isolation, solid fills, 44px `Done`, 64px microphone, handlers, and layout remain unchanged; local sample verification reports a `5s` duration with `infinite` iteration on the microphone and no disabled-button shine, while the normal local view reports the same cycle on enabled `Done` with no horizontal overflow; 34 test files and 123 tests, lint, TypeScript, and production build pass. No commit, push, merge, deployment, recording, task mutation, completion click, or paid AI request occurred.

- [x] Completed design-v2 Gate 2, Step 4 and closed the final quality gate. Evidence: an internal storage-feedback seam proves celebration is queued only after persistence and snapshot loading both succeed, either failure queues nothing, active/postponed statuses queue nothing, and the queue releases exactly once after replanning; source and automated audits confirm no Gate 2 JavaScript timer or event-listener loop, a stable reusable worker, reduced-motion runtime/CSS suppression, `aria-hidden` pointer-events-free effects, disabled `Done` without shine, and blocking-overlay layering at z-index 80 above the celebration canvas at 70; responsive local checks at 320, 360, 390, and 430px plus centered desktop confirm one canvas, a 44px preview target, a 64px microphone, 96px voice action, light/dark readability, brand-particle containment, no horizontal overflow, and restored light theme/normal viewport; the browser surface could not emulate reduced motion, so no visual reduced-motion pass is claimed; optimized route inspection finds one confetti-bearing chunk per independently loaded home or internal design-system route and no within-route duplication; 34 test files and 122 tests, lint, TypeScript, and production build pass. No commit, push, merge, deployment, recording, task mutation, completion click, or paid AI request occurred.

- [x] Completed design-v2 Gate 2, Step 3: restrained post-success completion confetti. Evidence: both current-task `Done` and dialog `Complete` queue one 36-particle Flownee-colored burst only after IndexedDB mutation and snapshot reload succeed; the burst waits until the blocking flow update closes, still acknowledges a stored completion if replanning later reports an error, and never triggers for failed storage, postpone, restore, edit, delete, clean-up, voice capture, rerender, or refresh; its canvas is `aria-hidden`, pointer-events-free, reduced-motion-disabled, worker-backed, and contained to the 430px app shell below blocking dialogs; the internal `/design-system` preview changes no task or API state; local light/dark previews confirm one canvas, a 44px preview target, readable brand particles, light-theme restoration, and no horizontal overflow; route-bundle inspection confirms one confetti implementation per independently loaded home or design-system route; 33 test files and 118 tests, lint, and production build pass. No commit, push, merge, deployment, recording, task mutation, completion click, or paid AI request occurred.

- [x] Completed design-v2 Gate 2, Step 2: restrained Shine Border integration. Evidence: the enabled current-task `Done` action and the 64px circular microphone control each receive one 1.5px violet/teal/light-blue shine pass lasting 2.5 seconds; the solid violet fills, control dimensions, action handlers, and fixed voice panel remain unchanged; the disabled `Done` action renders no shine; both overlays are `aria-hidden`, pointer-events-free, fade in and out, run exactly once, and are suppressed for reduced motion; local light/dark checks confirm a 44px `Done` target, 64px microphone, two approved overlays, unchanged accessible violet/white action colors, no horizontal overflow, and restoration of the original light theme; no action was clicked; 32 test files and 115 tests, lint, and production build pass. No commit, push, merge, deployment, recording, task mutation, or paid AI request occurred.

- [x] Completed design-v2 Gate 2, Step 1: minimal Magic UI foundation. Evidence: only the official MIT-licensed Shine Border and Confetti source primitives were adapted into `src/components/magicui`; their upstream attribution remains in source and design documentation; the Shine Border is pointer-events-free and suppressed for reduced motion; Confetti is staged with manual control and error isolation but is not connected to product behavior; only pinned `canvas-confetti@1.9.4` and `@types/canvas-confetti@1.9.0` were added, with the runtime package's ISC license recorded; no animation framework or unrelated Magic UI component was installed; production JavaScript chunks contain no confetti implementation because the staged primitives remain unused; 32 test files and 112 tests, lint, and production build pass. No visible UI effect, completion trigger, commit, push, merge, deployment, recording, task mutation, or paid AI request occurred.

- [x] Completed design-v2 Gate 1, Step 5: privacy, fixed-action clearance, responsiveness, and accessibility. Evidence: the home privacy note retains its exact meaning in an unboxed, divided presentation with a recognizable information icon; main content clearance now includes the device bottom safe-area inset and leaves approximately `23px` between the final privacy copy and fixed voice panel at the 320px test viewport; shared default, small, and icon button variants plus effort choices provide at least `44px` targets; long task-dialog titles wrap instead of truncating; the home page has one `h1` followed by logical `h2`/`h3` sections; visible icon-only controls retain accessible names; remaining voice/design-system animations include reduced-motion fallbacks; browser checks across 320-430px and light/dark states show no visible undersized targets or horizontal overflow; 31 test files and 110 tests, lint, and production build pass. No commit, push, merge, deployment, recording, task mutation, or paid AI request occurred.

- [x] Completed design-v2 Gate 1, Step 4: flattened supporting flow sections and states. Evidence: Up next and Saved items are semantic open sections with transparent backgrounds, no radius or shadow, and thin top/row dividers; the dashed upcoming empty box is replaced by simple muted text; saved titles wrap instead of truncating; task-management and bulk controls measure exactly `44px`; empty, loading, and completed presentations retain their wording, icons, loading semantics, and reduced-motion behavior in compact borderless sections without the former `25rem` cards; browser checks cover 320, 360, 390, and 430px plus light/dark rendering, local saved controls, voice-bar clearance, and zero horizontal overflow; 30 test files and 108 tests, lint, and production build pass. No commit, push, merge, deployment, recording, task mutation, or paid AI request occurred.

- [x] Completed design-v2 Gate 1, Step 3: open current-recommendation section. Evidence: the recommended action is now a semantic open section with no outer background, rounded container, border accent, or shadow; the existing status, updating state, emoji, title, effort, explanation, Done, Do later, and overflow controls remain intact; whitespace and two thin dividers provide hierarchy; the title uses responsive `24-30px` typography and wraps naturally; all three actions measure exactly `44px` high and remain on one row from 320-430px; light/dark browser checks show no horizontal overflow; 30 test files and 106 tests, lint, and production build pass. No commit, push, merge, deployment, recording, task mutation, or paid AI request occurred.

- [x] Completed design-v2 Gate 1, Step 2: mobile header and 44px touch targets. Evidence: the shared header remains approximately `64.8px` high; Help, Settings, and theme controls now provide exact `44x44px` targets while preserving their existing icon sizes, destinations, accessible labels, active-page styling, and theme behavior; the approved Flownee mark renders at `60x16.875px` with tighter wordmark spacing and responsive `12-16px` side padding; light and dark local checks across 320, 360, 390, and 430px show no overlap or horizontal overflow, including active Help and Settings pages; 30 test files and 106 tests, lint, and production build pass. No commit, push, merge, deployment, task mutation, recording, or paid AI request occurred.

- [x] Completed design-v2 Gate 1, Step 1: responsive baseline and Plus Jakarta Sans foundation. Evidence: Next.js now bundles and self-hosts the SIL Open Font License 1.1 typeface with system fallbacks and no runtime Google Fonts URLs; before/after measurements cover 320, 360, 390, and 430px; the font loads on home, Help, Settings, and design-system routes in light/dark operation; no horizontal overflow occurs; the fixed voice action remains 96px high and flush with the safe bottom edge; the wider font makes the sample current-task title four lines at 320px, recorded for the later title-layout step; 30 test files and 106 tests, lint, and production build pass. No component structure, spacing, sizing, behavior, commit, push, merge, or deployment changed.

- [x] Added AI-selected emoji cues to intentions. Evidence: GPT-5.6 planning contract version 2 requires exactly one fitting emoji grapheme for every newly extracted intention; runtime validation rejects words, mixed text, and multiple emoji; confirmed emojis persist with tasks while legacy IndexedDB records remain compatible through a neutral `✨` display fallback; icons render beside interpretation-review titles, the current recommendation, upcoming and saved intentions, and the management dialog without becoming a manual input; evaluation fixtures flag invalid emoji output; local light/dark browser checks confirm sample and legacy rendering in the centered `430px` shell with no horizontal overflow or task mutation; 30 test files and 106 tests, lint, and production build pass. Commit and deployment await separate product-owner approval.

- [x] Replaced the inline `Clean done` confirmation with a Flownee modal dialog. Evidence: the confirmation now uses the same responsive bottom-sheet/centered-dialog pattern as task management, with a dimmed blurred backdrop, destructive confirmation, cancel action, close icon, Escape cancellation, initial focus, body scroll lock, inert/`aria-hidden` underlying app, singular/plural copy, and in-dialog error recovery; atomic completed-item deletion remains unchanged; local `1280x720` browser checks confirm correct light/dark rendering, focused `aria-modal` semantics, hidden body overflow, no horizontal overflow, and safe cancellation that preserves all saved-item fixtures; 28 test files and 90 tests, lint, and production build pass. Commit and deployment await separate product-owner approval.

- [x] Made task-state replanning clearly visible and screen-blocking. Evidence: an accessible full-viewport modal overlay appears before local task mutation begins and remains through protected GPT-5.6 replanning; it states that the change is saved, uses the Flownee processing treatment, focuses itself, locks document scrolling, and makes the underlying app inert; it closes on success, handled failure, timeout, or when no active tasks remain; retry replanning uses the same overlay, while `Clean done` avoids it because no model call is needed; the safe fictional updating demo verifies a focused `aria-modal` dialog, inert/`aria-hidden` app, hidden body overflow, no dismiss button, exact `1280x720` viewport coverage at z-index 80, and no horizontal overflow without changing tasks or calling GPT-5.6; 28 test files and 90 tests, lint, and production build pass. Commit and deployment await separate product-owner approval.

- [x] Added `Clean done` and `Restore for later` bulk controls beneath `Saved items`. Evidence: both primary controls use the same blue-violet variant; unavailable actions are disabled by status count; cleaning completed items requires explicit confirmation and permanently removes all currently completed records in one IndexedDB transaction; restoring later items activates all currently postponed records in one transaction, one revision increment, one provisional plan, and one protected replan; dependency cleanup and stale-response guards remain intact; local light/dark browser checks confirm matching `rgb(82, 90, 255)` buttons after the list, no horizontal overflow, and a working clean confirmation that can be cancelled without changing the completed or postponed fixtures; 28 test files and 89 tests, lint, and production build pass. Commit and deployment await separate product-owner approval.

- [x] Crossed out completed titles in `Saved items`. Evidence: completed items receive the existing theme-aware muted text token plus a two-pixel line-through, while postponed items remain uncrossed; the existing `Done` and `Later` badges continue to communicate status independently of color or decoration; task management behavior is unchanged; server-rendered component coverage proves completed and postponed titles receive different treatments; no local user task was mutated for visual testing; 28 test files and 86 tests, lint, and production build pass. Commit and deployment await separate product-owner approval.

- [x] Unified the primary recommendation actions and added a clock to `Do later`. Evidence: `Done` and `Do later` now use the same blue-violet default button variant, while `Do later` includes the existing Flownee clock icon; action handlers, disabled states, spacing, wording, and the separate more-actions control remain unchanged; component coverage locks the shared variant and clock; local light/dark browser measurements confirm matching `rgb(82, 90, 255)` backgrounds, matching white text, a visible clock, and no horizontal overflow; 28 test files and 85 tests, lint, and production build pass. Commit and deployment await separate product-owner approval.

- [x] Reduced the voice-capture maximum from 90 seconds to 30 seconds. Evidence: the browser recorder's default automatic-stop timeout, recording-screen guidance, oversized-upload recovery message, MVP scope, implementation plan, and architecture cost controls all use the same approved 30-second limit; regression coverage locks the default duration and server guidance; local browser verification finds the exact 30-second message, no legacy 90-second copy, equal `597px` client and scroll heights, and no horizontal overflow; the dialog was closed before transcription or upload; 27 test files and 84 tests, lint, and production build pass. Commit and deployment await separate product-owner approval.

- [x] Added the approved breathing room around the Flownee capture illustration. Evidence: title-to-image spacing increases by `4px`, while two approved refinements increase image-to-microphone spacing by `8px` total; browser measurements confirm both gaps are now exactly `20px`; artwork, microphone, text, actions, colors, animation, and recording behavior remain unchanged; at the available `1280x720` viewport the dialog is approximately `599px` high with equal `597px` client and scroll heights, no internal scrollbar, and no horizontal overflow; it was closed without transcription or upload; 26 test files and 83 tests, lint, and production build pass. Commit and deployment await separate product-owner approval.

- [x] Compacted the initial voice-capture dialog so the complete recording state fits without scrolling. Evidence: the artwork frame now follows its landscape content instead of reserving a square layout box, while the rendered image width remains `365px`; dialog, title-to-artwork, artwork-to-microphone, microphone-to-status, action, and privacy spacing are reduced without changing component sizes, text, colors, or behavior; at the available `1280x720` viewport the centered `430px` dialog measures about `587px` high with equal `585px` client and scroll heights, no internal scrollbar, and no horizontal overflow; measured gaps are `16px`, `12px`, and `8px`; the recording dialog was closed without transcription or upload; 26 test files and 83 tests, lint, and production build pass. Commit and deployment await separate product-owner approval.

- [x] Replaced the initial voice-capture mock with the approved layered Flownee illustration and refined its presentation. Evidence: the product-owner-supplied `main.webp` remains fixed beneath four aligned transparent intention layers; the detail layer cycles in order every two seconds with a short opacity transition; the later approved refinement removes the large circular background, border, shadow, and clipping while preserving a responsive full-width artwork area; reduced-motion preference keeps the first intention static; the illustration remains limited to checking, microphone-request, and recording states; browser measurements confirm a transparent background, `0px` border and radius, no shadow or clipping, approximately two seconds between visible changes, and no horizontal overflow; the dialog was closed without transcription or upload; 26 test files and 83 tests, lint, and production build pass. Commit and deployment await separate product-owner approval.

- [x] Refined the initial voice-capture screen and reserved a larger future Flownee-image area. Evidence: the modal-only `Add by voice` eyebrow is removed while the fixed bottom entry action remains unchanged; `Tell Flownee what’s on your mind` is reduced to `20px`; the mobile sheet reserves `90svh` with a `96svh` maximum and safe vertical scrolling; a responsive 4:3 Flownee-image placeholder appears only during checking, microphone-request, and recording states, then disappears for transcription, review, planning, errors, and completion; local browser verification measures a `365x274` placeholder inside the centered `430px` sheet, confirms no horizontal overflow, and closes during microphone request without uploading audio; 26 test files and 82 tests, lint, and production build pass. Commit and deployment await separate product-owner approval.

- [x] Replaced the legacy installed-app icon with square variants of the approved Flownee wave logo. Evidence: deterministic, pixel-preserving compositions produce standard `192x192` and `512x512` PNGs, a safe-zone `512x512` maskable PNG, a `512x512` Next metadata icon, and a `180x180` Apple touch icon on the approved `#F8FAFC` neutral background; the web manifest references all standard and maskable variants; the service-worker icon cache is versioned to `flownee-shell-v2`; local manifest and all five icon endpoints return HTTP 200 with the expected PNG metadata; Next emits `/icon.png` and `/apple-icon.png`; 25 test files and 81 tests, lint, and production build pass. Installed copies may require uninstall/reinstall after deployment because operating systems cache PWA icons. Commit and deployment await separate product-owner approval.

- [x] Consolidated privacy and local-data management into Help and simplified the shared header. Evidence: the unexplained saved-status dot and privacy shield are removed from the home header; `/help#privacy-data` contains the full `Privacy & data` / `You stay in control` explanation, local/OpenAI data boundaries, request-limit notice, and the existing two-step IndexedDB deletion control; Settings links directly to that section; the obsolete home privacy query and dialog state are removed; browser verification confirms Help, Settings, and theme are the only home-header controls, the Settings link resolves to the anchored Help section, delete confirmation can be safely cancelled, and the centered desktop layout has no horizontal overflow; 24 test files and 80 tests, lint, and production build pass. Commit and deployment await separate product-owner approval.

- [x] Added the application version to Settings > About Flownee. Evidence: the displayed `App version 0.1.0` is supplied directly from the repository's `package.json` rather than duplicated as a hardcoded UI constant; a regression test requires the rendered value to match package metadata; mobile `390x844` visual inspection shows the version clearly separated within the About card and zero horizontal overflow; 24 test files and 80 tests, lint, and production build pass. Deployment awaits product-owner review.

- [x] Added dedicated Help and Settings screens with matching shared-header icons. Evidence: `/help` documents the voice-to-flow journey, capture tips, corrections, recording diagnostics, and privacy boundaries; `/settings` provides persistent light/dark choices, a safe link into the existing privacy/data dialog and confirmed delete-all workflow, AI-processing boundaries, and an About summary; reusable Help and Settings icon links appear in every header with accessible labels, tooltips, keyboard focus, and active-page state; the home order is Help, Settings, compact saved-status dot, privacy shield, and theme; mobile `390x844` measurements show four `36px` controls with no crowding, desktop `1024x900` has no header or page overflow, privacy deep-link closure cleans the query without deleting data, and theme selection remains synchronized; 24 test files and 80 tests, lint, and production build pass with both routes statically generated. Deployment awaits product-owner review.

- [x] Removed the remaining redundant labels from interpretation review. Evidence: `Item N` is now `Intention N`; the visible `Action` label is removed while each title input retains an accessible `Intention N action` name; the duplicate `Choose one` plus `Time effort` headings are consolidated into one `Choose time effort` legend; `AI estimate`, `Changed by you`, and `You stated this` badges are absent while provenance continues to be stored internally; the stated-deadline badge remains when relevant; component tests assert every wording and visibility requirement; 22 test files and 77 tests, lint, and production build pass. Deployment awaits product-owner review.

- [x] Simplified the post-interpretation review to the decisions users actually need to make. Evidence: each extracted intention now presents only its editable action title, six effort choices, effort provenance, and a stated-deadline badge when applicable; the optional-notes editor and all assumption labels, text, and checkboxes are removed; AI-generated notes continue to save automatically; low-impact assumptions remain automatic, while important uncertainty produces one generic `More detail is needed` blocker and directs the user to revise the transcript rather than silently accepting it; the primary action is now `Add to my flow`; component coverage proves removed controls and preserved safety behavior; 22 test files and 77 tests, lint, production build, and mobile/desktop shell overflow checks pass. Deployment awaits product-owner review.

- [x] Replaced the shared header mark with the second approved Flownee wave artwork. Evidence: the supplied `1536x1024` opaque PNG was converted into a real-alpha `1389x381` web asset with a 24 px transparent margin; opaque logo pixels, gradients, separate dot, and geometry remain unchanged; a preservation-locked generated extraction was rejected because it baked a checkerboard into the image; the cache-safe `/flownee-mark-v2.png` URL prevents Next/browser reuse of the earlier mark; the header renders it proportionally at `80x22` and the design reference at `116x32`; light, dark, mobile, and centered-desktop checks show no white rectangle, visible halo, distortion, or horizontal overflow; 21 test files and 75 tests, lint, and production build pass. The square PWA icon remains unchanged and deployment awaits product-owner review.

- [x] Added a persistent light/dark theme switch to the shared header. Evidence: a moon icon switches light to dark and a sun icon switches dark to light; accessible labels describe the resulting action; the control appears immediately to the right of the home privacy shield and as the sole right-aligned action on other pages; first visit follows the device preference, an explicit selection persists across navigation and reload, and the early initializer prevents a wrong-theme flash; the design-system-only text switch was removed; mobile and desktop browser checks show one theme control and zero horizontal overflow; 21 test files and 75 tests, lint, and production build pass. Deployment awaits product-owner review.

- [x] Integrated the approved Flownee wave mark into the shared application header. Evidence: the supplied transparent PNG was preserved without redraw and trimmed to a `530x171` web asset with a transparent safety margin; the shared header shows the mark beside the existing Flownee wordmark on every app page; the design-system reference includes the mark; the square PWA icon remains unchanged; light, dark, `390x844`, and `1024x900` browser checks show loaded images and no horizontal overflow; the linked home target retains a `44px` touch height; 20 test files and 72 tests, lint, and production build pass. Deployment awaits product-owner review.

- [x] Integrated the Flownee violet/teal/blue brand system with accessible light and dark themes. Evidence: centralized CSS tokens and Tailwind mappings; accessible action, badge, card, input, textarea, navigation, task-state, voice-recording, AI-processing, success, warning, and error treatments; violet logo/PWA metadata; no-index `/design-system` reference page; written color and contrast guidance; measured light/dark computed colors, mobile and desktop layouts, zero horizontal overflow, reduced-motion CSS, 20 test files and 72 tests, lint, and production build pass. Bottom navigation remains a reusable demo-only pattern and production deployment awaits product-owner review.

- [x] Replaced free-form effort entry with six AI-selected, user-editable time options. Evidence: GPT-5.6 output is restricted to `5`, `10`, `15`, `30`, `60`, or `120` minutes; the UI consistently displays these as `5′`, `10′`, `15′`, `30′`, `60′`, and `120′+`; one reusable accessible radio-button selector is used in interpretation review and task editing; AI/user/user-edited provenance remains visible; legacy custom values are preserved until explicitly edited; all home effort labels use the shared formatter; 66 tests, lint, production build, and local-browser display/overflow checks pass. Deployment awaits product-owner review and approval.

- [x] Added the shared Flownee header to every rendered app page. Evidence: reusable header uses the owned `/icon.svg` logo and linked Flownee wordmark, preserves the home privacy action, appears on the recording diagnostic, measures approximately `64px` high with a `36px` logo, aligns with page content at narrow and `1440px` widths, and has no horizontal overflow; 61 tests, lint, and production build pass. Deployment awaits product-owner review and approval.

- [x] Strengthened incremental planning so compatible intentions captured at different times are batched across the complete active-task snapshot. Evidence: each capture is proven to include every active task; GPT-5.6 must compare each new intention against all active tasks, reuse suitable context labels, and place compatible errands consecutively unless priority, deadlines, or dependencies override; the coffee-beans plus milk/fish/green-beans scenario is an executable evaluation fixture; production deploy `6a5cc8f228d4a4911924b2ee`; 59 tests, lint, and production build pass.

- [x] Fixed production same-origin validation behind Netlify's reverse proxy. Evidence: the public Flownee `Origin` is now compared with the externally visible host and forwarded protocol instead of Netlify's rewritten internal request URL; production deploy `6a5cc5ff68d958941cc7bed1` advances an audio-free same-origin request to normal HTTP 400 validation while an attacker-origin request remains HTTP 403; 57 tests, lint, and production build pass.

- [x] Restyled the bottom voice action as a classical rectangle and enlarged its title without overlap. Evidence: the panel has a measured `0px` corner radius, `Add by voice` is `16px`/semibold with a `20px` line height, and the unchanged `64px` microphone has `9px` of clear space above the title at narrow and `1440px` widths; no horizontal overflow; 54 tests, lint, and production build pass.

- [x] Extended the bottom `Add by voice` panel to the maximum available horizontal space. Evidence: the footer's horizontal padding is removed, so the action fills the footer content width at the narrow browser viewport and the complete `430px` desktop app shell; only the shell's `0.8px` border remains on each edge, with no horizontal overflow; 54 tests, lint, and production build pass.

- [x] Extended the unchanged-width `Add by voice` action downward into a full bottom panel. Evidence: the button keeps its existing top and horizontal position, grows from `80px` to `96px` when the baseline safe area is `16px`, reaches the viewport bottom at narrow and `1440px` widths, retains the `64px` microphone and `7px` title gap, uses a less pill-like `16px` radius, and shows no overflow or console errors; 54 tests, lint, and production build pass.

- [x] Refined the bottom voice action's text spacing without changing its structure or dimensions. Evidence: `Add by voice` is now `14px`/semibold, centered approximately `7px` below the unchanged `64px` microphone circle; subtitle position remains compact; narrow and `1440px` viewport checks show no overflow or console errors; 54 tests, lint, and production build pass.

- [x] Constrained the desktop application to a centered `430px` mobile-style shell, including the home content, single-column task flow, fixed voice bar, and modal widths. Evidence: desktop browser measurement at `1440x900` showed both app shell and voice bar at exactly `430px`, centered with no horizontal overflow or console errors; 54 tests, lint, and production build pass.

- [x] Replaced the small circular voice control with a large bottom-fixed call-to-action based on the product-owner sketch. Evidence: the full `Add by voice` control is clickable; its microphone medallion is centered across the top edge; the existing `Tap once and speak naturally` guidance remains inside the button; measured at approximately `343x80` CSS pixels on a `390x844` mobile viewport and `576x80` on desktop; no horizontal overflow or browser console errors; 54 tests, lint, and production build pass.

- [x] Conducted an expert usability walkthrough against the confirmed success measures and prepared the representative test protocol. Evidence: public first-run and fictional sample inspected in an isolated Chrome 150-compatible browser at `390x844`; single `64x64` voice control visible 16 px above the viewport bottom; no horizontal overflow; next action and reason understandable; no microphone/provider use; read-only sample-control ambiguity documented. This is not participant research, so the majority-participant measure remains open.

- [x] Deployed Flownee publicly to Netlify and ran the available supported-platform matrix. Evidence: production URL `https://flownee-build-week.netlify.app`; signed-out smoke coverage includes the home, fictional sample, manifest, service worker, icons, recording diagnostic, and enabled AI-status route; both paid routes reject untrusted origins with HTTP 403 before provider use; physical Android/iPhone journeys and Windows Chrome-engine recording/layout evidence are documented; the team completed final acceptance on every platform represented in the submission without claiming exhaustive browser/version certification.

- [x] Added the privacy notice, atomic delete-all-local-data control, request limits, throttling, and emergency kill switch. Evidence: responsive privacy/data panel with precise local/OpenAI data boundaries and two-step deletion; `/api/ai-status` preflight before microphone permission; authoritative paid-route switch checks; configurable per-client fixed-window limits with HTTP 429, `Retry-After`, and provider-call prevention; 54 passing tests, lint, production build, and browser verification of the mobile dialog and empty-origin deletion with no overflow or console errors. No live provider call was made.

- [x] Implemented complete, postpone, edit, delete, restore, and stale-response protection. Evidence: immediate atomic IndexedDB mutation with revision increments and dependency cleanup; valid local provisional plans; transcript-free GPT-5.6 replanning that cannot create tasks; abort plus revision-based late-response rejection; responsive manage/edit/delete confirmation UI; saved completed/postponed items; 47 passing tests, lint, production build, and 390×844 browser verification with no overflow or console errors. No live provider call was made.

- [x] Implemented interpretation review and automatic replanning. Evidence: protected same-origin `/api/plan` Responses route; explicit `gpt-5.6-sol`/`medium`, strict output parsing and refusal handling; confirmed-transcript-first recovery; editable extracted titles, notes, and effort; visible provenance, deadlines, assumptions, and clarifications; required assumption confirmation; atomic task/plan commit with temporary-reference mapping and revision checks; local home-state reload; 42 passing tests, lint, production build, and 390×844 browser shell verification with no overflow or console warnings. One bounded fictional live call reached OpenAI but returned HTTP 429, so successful live-output scoring remains outstanding.
- [x] Defined the GPT-5.6 planning contract and executable evaluation baseline. Evidence: explicit `gpt-5.6-sol`/`medium` Responses configuration, strict versioned input/output JSON Schemas, lean prompt, semantic rejection of unknown/duplicate/incomplete/inconsistent plans and invented deadline provenance, six representative fixtures including incremental replanning, empty speech, and transcript prompt injection, 33 passing tests, lint, and production build.
- [x] Implemented the protected GPT-4o Transcribe route and editable transcript-review flow. Evidence: server-only key and feature switch, same-origin browser protection, 6 MiB/type validation, 45-second provider timeout, safe errors, no-store responses, one-tap 90-second recorder, cancel/retry/re-record states, temporary Blob retention on failure, confirmed IndexedDB transcript persistence, permission-race protection, 22 passing tests, production build, and mobile production-browser verification with no console errors.
- [x] Established the browser recording and MIME-format strategy. Evidence: real Windows Chrome 150 engine recording passed with WebM/Opus, 47,639 bytes, 3,004 ms, and one audio track; Android support accepted by the product owner as a provisional development assumption with physical-device verification explicitly deferred to the cross-platform release gate.
- [x] Implemented the no-upload recording diagnostic and proved recording in the available Windows Chrome 150 engine. Evidence: secure context, microphone, and MediaRecorder APIs available; WebM/Opus preferred and produced; 47,639-byte Blob, 3,004 ms, one audio track; MP4 reported supported; Ogg reported unsupported; permission timeout and recovery guidance included; 15 tests, lint, and production build passing.
- [x] Defined and tested native IndexedDB schemas and repositories for transcripts, tasks, execution plans, and revision metadata. Evidence: runtime record validation, provenance fields, indexed version-1 stores, atomic task/plan commits, stale-plan rejection, snapshot loading, delete-all transaction, Apache-2.0 test dependency review, 12 passing tests, lint, and production build.

- [x] Implemented the responsive local-first PWA shell and home-screen states. Evidence: empty, saved-plan, updating, all-complete, loading, and browser-offline UI paths; fictional demo fixtures; install manifest, owned SVG icon, API-bypassing service worker; responsive browser checks at 1440×900 and 390×844 with no horizontal overflow or console errors; 6 unit tests, lint, and production build passing.
- [x] Scaffolded the shadcn/ui foundation without expanding product scope. Evidence: `components.json`, semantic Tailwind v4 theme tokens, tested `cn` utility, Button/Card/Badge primitives, permissive-license dependency review, and passing peer, lint, unit-test, and production-build checks.
- [x] Completed Milestone 0 — repository and compliance baseline. Evidence: public `main` repository now at https://github.com/vixfounder/flownee, initial commit `34af729`, clean dependency install, and passing peer, lint, test, build, and credential-scan checks.
- [x] Confirmed project idea, audience, problem evidence, value proposition, MVP, architecture direction, team, platform, privacy model, success measures, and repository strategy. Evidence: product discovery conversation completed July 18, 2026.
- [x] Created the minimum pre-development Markdown documentation set. Evidence: files under `docs/` plus root `AGENTS.md`.
- [x] Initialized the local Git repository on `main`. Evidence: local `.git` metadata created July 18, 2026.
- [x] Established the Next.js TypeScript baseline with Tailwind CSS, ESLint, Vitest, pnpm lockfile, MIT License, safe `.env.example`, and setup README. Evidence: `pnpm lint`, `pnpm test`, and `pnpm build` all pass.
- [x] Connected the verified public GitHub repository. Evidence: https://github.com/vixfounder/flownee.

## Optional post-submission evidence

- Fixture-level GPT-5.6 token use, latency, and final reasoning comparison. Successful live voice-to-AI planning is now product-owner-reported across physical Android and iPhone journeys; the earlier bounded fixture request returned HTTP 429 on July 18, 2026.
- Supported audio MIME-format results per browser.
- Representative-participant observations from the ongoing optional usability study.

## Session handoff

- Current state: Flownee is submitted at https://devpost.com/software/flownee; the complete local-first product journey is publicly deployed; every MVP acceptance criterion and automated gate passes; the Codex Session ID, public 2:36 video, intended-platform acceptance, rights confirmation, judge-period credits, and signed-out URL audit are recorded.
- Next action: Continue the optional post-submission representative usability study and record observed outcomes without changing the completed hackathon claims.
- Known failures: No current code, MVP acceptance, intended-platform, release, or submission failure is known. Exact mobile browser versions, exhaustive per-browser MIME diagnostics, fixture-level model-performance measurements, and the ongoing optional participant results remain bounded follow-up evidence.
- Uncommitted decisions: Any post-hackathon change to GPT-5.6 reasoning effort requires fixture-level latency, token, and quality evidence.
